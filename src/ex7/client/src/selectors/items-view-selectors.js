const getItemsView = (state) => state.itemsView;

// export const selectTotalCompletedTodos = (state) => {
//   getItemsView(state).todos.filter((todo) => todo.completed);
// };

// export const filterTodo = (todos, searchKeyword) => (
//     todos.filter(todo => todo.title.toLowerCase().includes(searchKeyword.toLowerCase()))
//   )

// export const filterTodo = (state, searchKeyword) =>
//   getItemsView(state).todos.filter((todo) =>
//     todo.name.toLowerCase().includes(searchKeyword.toLowerCase())
//   );
